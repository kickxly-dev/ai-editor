{
  "framework": "nextjs",
  "buildCommand": "npx drizzle-kit push --yes && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/scrape",
      "schedule": "0 6 * * *"
    }
  ],
  "experimentalServices": {
    "courtiq-2k26": {
      "type": "web",
      "routePrefix": "/",
      "framework": "nextjs",
      "buildCommand": "npm install && npm run build"
    }
  }
}
